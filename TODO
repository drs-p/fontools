autoinst:
-   sort list of fonts by family before creating cmdlines
-   create texmf-dirs only once per family
-   math fonts?

testcases:
-   open source fonts only
-   more widths (esp compressed, extended, narrow, wide)
-   test some families (eg alegreya) without Regular fonts
